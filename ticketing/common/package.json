{
  "name": "@sjoedwards/common",
  "version": "1.0.12",
  "description": "",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "build": "echo \"Clean and Build Package\" \n yarn run clean && tsc",
    "pub": "yarn version --patch && yarn run build && npm publish",
    "preversion": "echo \"Version: Increment version and commit => create tag\n\"",
    "postversion": "echo \"Post Version: Push tags to Github\n\" git push --tags",
    "prepublishOnly": "echo \"Publishing latest version to npm\"",
    "version": "echo version"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.1.3"
  },
  "devDependencies": {
    "del-cli": "^3.0.1"
  }
}
