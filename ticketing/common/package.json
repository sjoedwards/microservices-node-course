{
  "name": "@sjoedwards/common",
  "version": "1.0.14",
  "description": "",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "build": "echo \"Clean and Build Package\" \n yarn run clean && tsc",
    "pub": "yarn version --patch && yarn run build && npm publish --access public",
    "postversion": "git push --tags"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.1.3"
  },
  "devDependencies": {
    "del-cli": "^3.0.1"
  }
}
