{
  "name": "@sjoedwards/common",
  "version": "1.0.8",
  "description": "",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "build": "yarn run clean && tsc",
    "pub": "yarn version --patch && yarn run build && yarn publish",
    "preversion": "echo preversion",
    "version": "echo version",
    "postversion": "echo Pushing Tags...\n git push --tags"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.1.3"
  },
  "devDependencies": {
    "del-cli": "^3.0.1"
  }
}
